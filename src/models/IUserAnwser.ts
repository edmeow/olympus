export interface IUserAnwser {
    comment: string | null;
    fileContent: string;
    id: string;
    points: string | null;
    sentTime: Date;
    session: number;
    taskNumber: number;
    userId: number;
}
